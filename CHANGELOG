- version: 2.0.3
    - glennfu: added support for title's with \r's in them
- version: 2.0.3
    - glennfu: re-wrote .serializeArray() instead of .serialize() since it'll catch more use cases for submission
- version: 2.0.2
    - glennfu: hooked into the .serialize() method so you can submit forms with JS and let labelify clean the form fields
    - glennfu: correctly extended jQuery to avoid conflicts with other JS libraries using function($)

- version: 2.0.1
    - glennfu: renamed '$' to 'jQuery' to avoid conflicts with Prototype
    - glennfu: swapped order of class adding to remove the flicker
    - glennfu: default value reverted to is always a blank string
 
- version: 2.0 (original)
    - Forked from garrett's source